/** @type {import('next').NextConfig} */
const nextConfig = {
  // No special configuration needed
  // Remove all webpack config since you're not using server-side FFmpeg
}

module.exports = nextConfig